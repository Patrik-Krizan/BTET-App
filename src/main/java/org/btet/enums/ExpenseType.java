package org.btet.enums;

/**
 * Enum for ExpenseType which can be used to categorize expenses, found in the Expense class.
 *TRANSPORT, ACCOMMODATION, REPRESENTATION, OTHER.
 * */
public enum ExpenseType {
    TRANSPORT, ACCOMMODATION, REPRESENTATION, OTHER
}
