package org.btet.enums;
/**
 * Enum for UserRole which can be used to categorize users in login, found in the User abstract class.
 * ADMIN, EMPLOYEE, MANAGER.
 * */
public enum UserRole {
    ADMIN, EMPLOYEE, MANAGER;
}
